apiVersion: batch/v1
kind: CronJob
metadata:
  name : analysisjob-13
  labels:
    binance: analyze
spec:
  schedule: "*/1 * * * *"
    #schedule: "13 7 */15 * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: analysis-13
            image: karthikdhanusu/gatherdataimg:v1.2
            imagePullPolicy: Always
            command: ["sh", "-c", "mkdir mastertech && cp -f /binance/gatherdata/src/techmaster/techmaster13.txt /binance/gatherdata/src/mastertech/ && mv /binance/gatherdata/src/mastertech/techmaster13.txt /binance/gatherdata/src/mastertech/techmaster.txt && python3.8 ./masteranalysis.py"]
            volumeMounts:
            - name: binancevol 
              mountPath: "/mnt/binance"
          volumes:
          - name: binancevol
            persistentVolumeClaim:
              claimName: binance-pvc-hs
          restartPolicy: Never
          imagePullSecrets:
          - name: kdhanusu
          
